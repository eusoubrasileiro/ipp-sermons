#!/usr/bin/env sh
# Fast gate. Biome auto-fix on staged files, then full typecheck + unit tests
# across the workspace.
#
# The heavier stages from the house template (coverage ratchet against
# quality-baseline.json, LLM security review) are deliberately not wired up
# yet -- the frontend package does not exist, so there is no meaningful
# baseline to ratchet against. Add them once Phase 5 lands.
pnpm exec lint-staged
